import './apis.css'

function Apis(){
    const empty = "At this Moment we Don't have any API's :(";

    return(
        <section id="section1a">
            <h1>{empty}</h1>
        </section>
    )
}

export default Apis;